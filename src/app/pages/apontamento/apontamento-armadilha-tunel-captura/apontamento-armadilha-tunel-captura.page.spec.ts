import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ApontamentoArmadilhaTunelCapturaPage } from './apontamento-armadilha-tunel-captura.page';

describe('ApontamentoArmadilhaTunelCapturaPage', () => {
  let component: ApontamentoArmadilhaTunelCapturaPage;
  let fixture: ComponentFixture<ApontamentoArmadilhaTunelCapturaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ApontamentoArmadilhaTunelCapturaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
