import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ApontamentoArmadilhaFeromonioPage } from './apontamento-armadilha-feromonio.page';

describe('ApontamentoArmadilhaFeromonioPage', () => {
  let component: ApontamentoArmadilhaFeromonioPage;
  let fixture: ComponentFixture<ApontamentoArmadilhaFeromonioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ApontamentoArmadilhaFeromonioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
