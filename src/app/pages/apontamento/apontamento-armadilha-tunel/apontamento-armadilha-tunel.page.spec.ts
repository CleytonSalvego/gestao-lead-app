import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ApontamentoArmadilhaTunelPage } from './apontamento-armadilha-tunel.page';

describe('ApontamentoArmadilhaTunelPage', () => {
  let component: ApontamentoArmadilhaTunelPage;
  let fixture: ComponentFixture<ApontamentoArmadilhaTunelPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ApontamentoArmadilhaTunelPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
